# Convenience rewrites: bare paths → static JSON (200 rewrite).
# NOTE: /api/v1/health is served by functions/api/v1/health.ts when Functions
# are enabled — do not rewrite that path so the live handler wins.
/api/v1/project   /api/v1/project.json   200
/api/v1/stack     /api/v1/stack.json     200
/api/v1/phases    /api/v1/phases.json    200
/api/v1/endpoints /api/v1/endpoints.json 200
